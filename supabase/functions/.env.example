# Copie para `supabase/functions/.env` no desenvolvimento local.
# Este arquivo real é ignorado pelo Git e carregado automaticamente por `supabase functions serve`.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
